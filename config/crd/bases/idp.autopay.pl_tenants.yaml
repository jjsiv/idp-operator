---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: tenants.idp.autopay.pl
spec:
  group: idp.autopay.pl
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantName
      name: Tenant name
      type: string
    - jsonPath: .status.provisioned
      name: Provisioned
      type: string
    - jsonPath: .status.backstageGroup
      name: Backstage group
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Tenant
            properties:
              backstageCatalog:
                description: Configuration for provisoning Backstage Software catalog
                  Entities associated with this Tenant.
                properties:
                  provisioning:
                    description: Strategy for provisioning Backstage Software catalog
                      entity data.
                    properties:
                      git:
                        description: |-
                          Provision entity data as file in a Git repository.
                          This is the only supported option currently.
                        properties:
                          branch:
                            description: Branch to push to. If unset, default will
                              be assumed.
                            type: string
                          commitMessage:
                            description: Message for the commit that will be created.
                            type: string
                          config:
                            description: Configuration for repository connection.
                            properties:
                              auth:
                                description: Authentication information for cloning
                                  and pushing to the repository.
                                properties:
                                  secretRef:
                                    description: Reference to a Kubernetes secret.
                                      Must be found in the same namespace.
                                    properties:
                                      key:
                                        description: Key containing the private key
                                          for authentication.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the secret.
                                          If unset, template's namespace will be used.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                required:
                                - secretRef
                                type: object
                              author:
                                description: Information about commit author.
                                properties:
                                  email:
                                    description: Commit author's email.
                                    type: string
                                  name:
                                    description: Commit author's name.
                                    type: string
                                required:
                                - email
                                - name
                                type: object
                            required:
                            - auth
                            - author
                            type: object
                          path:
                            description: Path (full directory + filename) to create
                              catalog entities.
                            type: string
                          repositoryURL:
                            description: URL of the repository to create files in.
                            type: string
                        required:
                        - commitMessage
                        - config
                        - path
                        - repositoryURL
                        type: object
                    required:
                    - git
                    type: object
                required:
                - provisioning
                type: object
              description:
                description: Description about this resource.
                type: string
              displayName:
                description: Human readalbe name of the resource.
                type: string
              identityProvider:
                description: Configuring an identity provider allows for creating
                  of identity groups for members of this project.
                properties:
                  okta:
                    description: Configuration for Okta integration.
                    properties:
                      groups:
                        description: Groups to create.
                        items:
                          properties:
                            name:
                              description: Name of the group to create.
                              type: string
                            roles:
                              description: Only members with the specified roles will
                                be added to this group.
                              items:
                                type: string
                              type: array
                          required:
                          - name
                          type: object
                        type: array
                    required:
                    - groups
                    type: object
                type: object
              members:
                description: Members of this Tenant.
                items:
                  properties:
                    email:
                      description: Email address of the user.
                      type: string
                    entityName:
                      description: Name of the User Entity in Backstage.
                      type: string
                    roles:
                      description: Roles to assign to this member.
                      items:
                        type: string
                      type: array
                  required:
                  - email
                  - entityName
                  type: object
                type: array
            required:
            - backstageCatalog
            - members
            type: object
          status:
            description: status defines the observed state of Tenant
            properties:
              backstageGroup:
                description: Top level Backstage group created from this Tenant.
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the Tenant resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              provisioned:
                description: Provisioned indicates whether the Tenant has been successfully
                  provisioned.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
