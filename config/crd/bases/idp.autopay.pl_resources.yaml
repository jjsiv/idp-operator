---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: resources.idp.autopay.pl
spec:
  group: idp.autopay.pl
  names:
    kind: Resource
    listKind: ResourceList
    plural: resources
    singular: resource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Resource name
      type: string
    - jsonPath: .spec.tenantRef.name
      name: Owner
      type: string
    - jsonPath: .spec.templateRef.name
      name: Template
      type: string
    - jsonPath: .status.provisioned
      name: Provisioned
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Resource is the Schema for the resources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Resource
            properties:
              backstage:
                description: |-
                  Specification for Backstage Software Catalog Resource to create from this Resource.
                  Resource owner will be set to the Tenant Backstage Group.
                  Type will be set to ResourceTemplate's name.
                  Parameters passed to the template as well as other values will be exposed as annotations.
                properties:
                  create:
                    description: Create defines whether the Backstage Resource should
                      be created.
                    type: boolean
                type: object
              description:
                description: Description about this resource.
                type: string
              displayName:
                description: Human readalbe name of the resource.
                type: string
              templateRef:
                description: Reference to a ResourceTemplate that this Resource will
                  be provisioned from.
                properties:
                  name:
                    description: Name of the ResourceTemplate.
                    type: string
                  namespace:
                    description: Namespace of the ResourceTemplate. If not specified,
                      it is assumed to be in the same namespace as the Resource.
                    type: string
                  parameters:
                    description: Parameters to pass to the template.
                    items:
                      description: ResourceParameter is a spec for parameters to pass
                        to a template.
                      properties:
                        name:
                          description: Name of the parameter.
                          type: string
                        value:
                          description: Value for the parameter.
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                required:
                - name
                type: object
              tenantRef:
                description: Reference to a Tenant that will be set as the owner of
                  this Resource.
                properties:
                  name:
                    description: Name of the Tenant resource in the same namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - templateRef
            - tenantRef
            type: object
          status:
            description: status defines the observed state of Resource
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Resource resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              provisioned:
                description: Provisioned indicates whether the Resource has been successfully
                  provisioned.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
